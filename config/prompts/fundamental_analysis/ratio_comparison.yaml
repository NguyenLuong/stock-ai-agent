# Fundamental Analysis Agent — ratio comparison prompt
# TODO: Story 1.4 — implement full Jinja2 template
name: fundamental_analysis_ratio_comparison
version: "1.0"
model_key: triage
template: |
  # PLACEHOLDER — Story 1.4
  Compare financial ratios for {{ ticker }} vs sector: {{ ratios }}
