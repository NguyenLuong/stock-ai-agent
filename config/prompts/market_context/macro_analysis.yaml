# Market Context Agent — macro indicators prompt
# TODO: Story 1.4 — implement full Jinja2 template
name: market_context_macro_analysis
version: "1.0"
model_key: triage
template: |
  # PLACEHOLDER — Story 1.4
  Analyze macro indicators: {{ macro_data }}
